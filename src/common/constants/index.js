export const cookiesConsts = {
    access_token:'access_token',
    authorization:'Authorization',
    id_token:'id_token',
    email:'email',
    time_zone:'time_zone',
    userName : 'userName'
}

export const ApiConsts={
  
}


export const ValidationConfig={
    ReasonFieldMaxChars:240,
    InputPatern: '^[a-zA-Z0-9 .?():,;\'"_-]+$'
}

export const Message={
    MaxLength:'Maximum #count# characters allowed.',
    CharValidation:"Only alphabets, numbers and special characters(.?():,;'\"_-) are allowed"
}